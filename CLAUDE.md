# CLAUDE.md — проект "Симулятор самолетного БПЛА"

Контекст для Claude Code. Прочитать в начале каждой сессии. Здесь — постоянные
правила проекта; конкретику задачи смотри в `TZ_simulator.md`, физику — в
`Физика_справочник_BeardMcLain.md`.

## Что это за проект

Имитационная модель (симулятор) ЛА самолетного типа для магистерской работы
"Разработка зонда измерения аэродинамических характеристик БПЛА". Симулятор —
инструмент главы 8: должен дать графики и числа, доказывающие, что прямое
измерение угла атаки зондом улучшает работу САУ по сравнению с косвенной оценкой.

Автор — руководитель проекта, специалист по БПЛА и ТАУ, отвечает за технические
решения и проверяет код. Claude Code — исполнитель: пишет и запускает код по ТЗ.
Научные развилки, не описанные в ТЗ, не решать молча — выносить автору.

## Структура проекта

```
sim/                  — ядро физики (изолировано, без управления)
    config.py         — параметры ЛА, ветра, датчиков, прогона (AircraftParams и др.)
    state.py          — вектор состояния, именованные индексы, air_velocity
    dynamics.py       — derivatives(), thrust()  [чистая функция]
    aero.py           — aero_forces_moments(), coef_CL/CD/Cm
    integrators.py    — step_euler(), step_rk4()
    wind.py           — wind(h, t, params)

control/              — управление (импортирует только sim/)
    controllers.py    — PitchController, SpeedController, PID, PIDParams
                        PitchControlParams.gain_scheduling — масштабирование delta_e ∝ (Va_ref/Va)²
    sensors.py        — measure_gyro/altitude/airspeed/angle_of_attack/gps_*
    estimators.py     — estimate_alpha_indirect()

runner.py             — главный цикл: run(), Log, compute_trim(), trim_state(), print_summary()
flight_logger.py      — FlightLogger, load_log(), print_log_info()

viz/                  — отображение (принимает Log, не знает физики)
    plotting.py       — plot_dynamics/trajectory/energy/integrator_check()
    animate.py        — animate_log()  [силуэт ЛА, анимация по Log]
    viewer.py         — FlightLogViewer; CLI: python viz/viewer.py [файл.flightlog]

scenarios/            — прогоны s1–s9; каждый: конфиг → run() → viz или logger.save()
    s1_steady_flight.py    — балансировка, тримовый горизонтальный полёт
    s2_pitch_up.py         — кабрирование (уставка theta_ref > 0)
    s3_pitch_down.py       — пикирование (уставка theta_ref < 0)
    s4_stall.py            — выход на закритический УА + индикация (ТЗ С4)
    s5_altitude_control.py — контроль высоты (P-контур h → theta_ref → PID theta+q)
    s6_speed_control.py    — контроль высоты + удержание Va (SpeedController)
    s7_alpha_estimation.py — сравнение оценок УА: зонд vs ИНС+GPS при попутном ветре
    s8_energy_comparison.py  — то же + субплот накопленной энергии (прокси throttle·Va)
    s8_b_motor_work.py     — ФИЗИЧЕСКИ КОРРЕКТНАЯ работа мотора W=∫T·Va·dt [Дж];
                             gain_scheduling включён; ретрим на VA_REF
    s9_paired_probe_comparison.py — парный прогон "с зондом vs без зонда"

checks/               — проверки здравого смысла
    check.py              — 26 проверок всех модулей sim/; запуск: python checks/check.py
    check_polar.py        — аэродинамическая поляра
    check_motor.py        — характеристики силовой установки
    check_optimal_speed.py — Va_md (макс. дальность), Va_mp (макс. продолжительность),
                             what-if анализ: какой параметр сдвигает Va_md к заданной цели

docs/                 — техническая документация
results/              — .flightlog файлы (генерируемые, не коммитить)
```

## Правила импорта

Внутри пакетов — относительные импорты (`from .config import ...`).
Из сценариев/проверок — абсолютные после `sys.path.insert(0, "..")`:
```python
from sim.config import AircraftParams, WindParams, SimConfig, SensorParams
from sim.state  import THETA, Q, H, X, U, W
from control.controllers import PitchController, PitchControlParams
from control.sensors     import measure_gyro, measure_airspeed, ...
from control.estimators  import estimate_alpha_indirect
from runner       import run, compute_trim, trim_state, print_summary
from flight_logger import FlightLogger
from viz.animate  import animate_log
from viz.plotting import plot_dynamics
```

## Опорные файлы (в репозитории)

- `TZ_simulator.md` — техническое задание: архитектура, модули, сценарии, этапы.
  Главный документ. Реализация идет по этапам из раздела 10 ТЗ.
- `Физика_справочник_BeardMcLain.md` — уравнения движения, аэродинамика,
  параметры-аналоги (Zagi/Aerosonde), параметры датчиков. ВНИМАНИЕ: это OCR
  русского перевода, часть формул битая — использовать как ориентир по
  структуре, при сомнении брать стандартные уравнения 6DOF/продольного канала.
- `Simulator.py` — авторский симулятор квадрокоптера. ЭТАЛОН СТИЛЯ: классы PID,
  Saturation, каскад регуляторов, формат прогона и графиков. Новый код держать в
  этом стиле, но состояние — единым numpy-вектором (см. ниже).

## Технологический стек (жестко)

- Python 3, только numpy и matplotlib. Никаких сторонних физических движков
  (MuJoCo, JSBSim и т.п. в код не тянуть — решение принято осознанно).
- Интегратор пишется вручную (RK4 основной, Эйлер опционально).
- scipy допустим только при явной необходимости и с согласования автора.

## Архитектурные правила (не нарушать)

1. Физика отделена от численного метода. Вся динамика — в чистой функции
   `derivatives(state, controls, t) -> dstate`, без присваиваний к внешнему
   состоянию. Интеграторы (`step_euler`, `step_rk4`) — отдельно, взаимозаменяемы.
2. Состояние — единый numpy-вектор с именованным доступом к компонентам (без
   "магических индексов"). Не хранить состояние россыпью атрибутов объекта.
3. Параметры ЛА/среды/датчиков — в отдельном конфиге, легко подменяемом. Код не
   содержит "зашитых" чисел физики.
4. Управление заморожено на шаг интегрирования (внутри k1..k4 не пересчитывается)
   — это соответствует дискретной природе САУ.
5. Модульность реализована пакетами: `sim/` (физика) → `control/` (управление) →
   `runner.py` / `flight_logger.py` → `viz/` (отображение) → `scenarios/` (прогоны).
   Каждый слой знает только о нижележащих. `viz/` не импортирует `control/`.
   Боковой канал добавляется расширением вектора состояния, не переписыванием продольного.
6. Визуализация полностью отделена от прогона: сценарий отдаёт `Log` в `viz.*`
   или сохраняет `.flightlog` через `FlightLogger`. Анимацию и графики менять
   только в `viz/`, не трогая сценарии.

## Терминология (глоссарий проекта — единообразно в коде и комментариях)

- САУ — система автоматического управления (НЕ "автопилот").
- ЛА — летательный аппарат.
- УА — угол атаки (alpha). УС — угол скольжения (beta). АУ — аэродинамические
  углы (УА и УС вместе).
- ИНС — инерциальная навигационная система (гироскоп + акселерометр, дают углы
  ориентации).
- СВС — система воздушных сигналов. ПВД — приемник воздушного давления.
- АДХ — аэродинамические характеристики (зависимости Cl, Cd, Cm от АУ).
- Зонд — съемный датчик прямого измерения АУ (центральный объект работы).
- Коэффициенты — ТОЛЬКО английская нотация: Cl (подъемная сила), Cd
  (сопротивление), Cm (момент тангажа).
- Критический УА — граница срыва (падение Cl, рост Cd), задает скорость
  сваливания. Наивыгоднейший УА — режим max Cl/Cd.

## Ключевые требования к содержанию (из ТЗ)

- УА определяется по ВОЗДУШНОЙ скорости (с учетом ветра), не по земной. Это
  принципиально для эффекта зонда.
- Псевдодатчики — с шумом и смещением (иначе зонд не отличить от истины).
- "Мир без зонда": косвенная оценка УА как theta - gamma (тангаж с ИНС, угол
  траектории с GPS). Помечать как моделирование, не эксперимент.
- Парный прогон "с зондом vs без": обе САУ ИДЕНТИЧНЫ во всем (ПИД, сценарий,
  ветер, шум общих датчиков), различие только в источнике УА. Зашить это в код.
- Индикация выхода на закритический УА — обязательна (требование плана работы).

## Правила работы с автором

- НЕ выдумывать числовые результаты, измерения, источники. Нет данных — брать
  значения-аналоги (Aerosonde) и явно помечать "параметры-аналог, заменяются",
  либо спрашивать автора. Места для данных автора в ТЗ помечены `[АВТОР: ...]`.
- Различать "моделирование" и "эксперимент": симулятор дает моделирование.
- Не утверждать ничего о реальном железе автора сверх того, что задано в конфиге
  или сказано явно.
- Перед написанием derivatives — сначала выписать реализуемые уравнения (это
  материал для приложения Б диссертации), затем код.
- Код должен быть понятен автору (он защищает каждую строку). Никакой "магии" —
  при усложнении объяснять или упрощать.
- Оформление под ГОСТ и текст диссертации НЕ делать в Claude Code — это
  отдельная задача в чате проекта.

## Текущий статус реализации

### Реализовано (Этап 0 + Этап 1 + Этап 2 полностью)
- Ядро физики: `sim/` — dynamics, aero (sigmoid CL), integrators (RK4+Euler), wind, state
- Управление: `control/` — PitchController (theta+q cascade), SpeedController (Va ПИД),
  псевдодатчики, `estimate_alpha_indirect()` (ИНС+GPS)
- **Gain scheduling в PitchController** (`gain_scheduling=True` в `PitchControlParams`):
  `delta_e *= clip((Va_ref/Va_meas)², gs_scale_min, gs_scale_max)` — компенсирует
  падение эффективности руля высоты при Va ≠ Va_ref. Va_ref = скорость НАСТРОЙКИ ПИД
  (не полёта!). По умолчанию выключен (`False`) — обратная совместимость сохранена.
- Инфраструктура: `runner.py`, `flight_logger.py` (.flightlog / npz), `viz/` (plotting,
  animate, viewer)
- Сценарии s1–s9: балансировка, кабрирование, пикирование, срыв с индикацией,
  контроль высоты, контроль Va, сравнение оценок УА, метрика энергии
- **s8_b `scenarios/s8_b_motor_work.py` — физически корректная работа мотора**:
  - Метрика `W = ∫T(t)·Va(t) dt` [Дж] вместо прокси `throttle·Va`
  - T вычисляется из модели: `T = 0.5·ρ·S_prop·C_prop·((k_motor·δt)²−Va²)`
  - `gain_scheduling=True`, `Va_ref=30.0` (точка настройки), ретрим на `VA_REF`
  - Субплоты: P_мот(t) [Вт] и W(t) нарастающий [Дж]
- **s9 `scenarios/s9_paired_probe_comparison.py` — парный прогон "с зондом vs без зонда"**
  (кульминационный результат, Этап 2 ТЗ С6):
  - Два независимых `run()` с идентичными ПИД/ветром/шумом общих датчиков
  - УА **подаётся в контур управления**: `theta_ref = alpha_src + KH * h_err`
  - Честный RNG: `rng_common` seed=42 в обоих прогонах, `rng_probe` seed=99 независим
  - 4 пресета условий (раскомментировать нужный): встречный ветер, сдвиг ветра,
    порыв, штиль (контрольный)
  - Метрики: J_E, J_h, J_safe, J_eff; анимация с двумя наложенными траекториями
- **`checks/check_optimal_speed.py`** — аналитический и численный расчёт:
  - Va_md (макс. качество L/D, мин. тяга, макс. дальность)
  - Va_mp (мин. мощность, макс. продолжительность) = 0.76·Va_md
  - What-if: какую массу / CDp / площадь крыла взять чтобы Va_md = заданная цель

### Текущие параметры ЛА (sim/config.py)
- `mass = 10 кг` [АВТОР] — обновлено; даёт Va_md = 25.1 м/с (оптимум L/D)
- `Jy = 1.135 кг·м²` — **требует обновления** под m=10 кг (оценка: ~2.27 кг·м²) [АВТОР]
- Остальные аэродинамические и силовые параметры — аналог Aerosonde, подлежат замене

### Оптимальные режимы (m=10 кг, текущий конфиг)
| Режим | Va, м/с | alpha, ° | P_мот, Вт |
|-------|---------|----------|-----------|
| Va_md (макс. L/D, макс. дальность) | **25.1** | 5.8° | 481 |
| Va_mp (мин. мощность, макс. продолж.) | 19.1 | 11.2° | 422 |
| Va=30 (точка настройки ПИД) | 30.0 | ~2° | ~900 |
| Va_stall | 14.4 | — | — |

### Результаты s9 (параметры-аналог, m=5 кг, Пресет 1) — **устарело, m изменён**
Условия: постоянный встречный ветер Vwx = −5 м/с, рамп-снижение 150→100 м за 60 с.

| Метрика | С зондом | Без зонда | Δ |
|---------|----------|-----------|---|
| J_E [∫thr dt, о.е.·с] | 37.43 | 37.43 | 0% |
| **J_h [СКО h−h_ref, м]** | **4.51** | **5.16** | **+12.6% лучше** |
| J_eff [∫Va dt / J_E, м/о.е.] | 72.1 | 72.1 | 0% |

Результаты получены при m=5 кг. После обновления Jy и верификации s9 — перезапустить.

### Не реализовано из ТЗ
- ТЗ С5: сравнение двух установившихся режимов с разным Cl/Cd
- s9_b: парный прогон с метрикой реальной работы мотора T·Va (запланирован)
- Штилевой контрольный прогон вынесен в Пресет 4 s9 (раскомментировать)

## Порядок работы (рекомендуемый)

- Идти по этапам ТЗ (0 каркас → 1 база → 2 обязательный слой → 3 фичи).
- После каждого этапа: запустить, показать графики, дождаться проверки автором,
  закоммитить. Не делать несколько этапов одним большим непроверяемым куском.
- Сначала отладка на параметрах-аналоге (Aerosonde), подмена на авторские данные
  — после того как архитектура работает.
- Добавлять простые проверки здравого смысла (в установившемся режиме состояния
  постоянны; при нулевых руле и тяге ЛА снижается; рост УА повышает Cl до срыва).

## Известные ограничения (не трогать без согласования)

### Адаптивность PitchController к скорости полёта (gain scheduling)

**Реализовано частично.** `PitchControlParams.gain_scheduling=True` масштабирует
выход q-контура: `delta_e *= clip((Va_ref/Va_meas)², 0.25, 9.0)`.
`Va_ref` = скорость настройки ПИД (= 30 м/с), а не скорость полёта.
Это компенсирует изменение аэродинамической эффективности руля ∝ Va².

**Что не охвачено gain scheduling:**
- theta-контур (внешний): полоса пропускания не масштабируется по Va
- SpeedController: feedforward `thr_trim` не пересчитывается на новой Va_ref
- При сильном отклонении от точки настройки (Va < 20 или Va > 40 м/с)
  нужен полный ретрим (`compute_trim(aircraft, Va_actual)`) + обновлённый thr_trim

**Рабочий диапазон с gain_scheduling=True**: Va = 20–35 м/с (scale = 0.73–2.25).
За пределами этого диапазона качество управления деградирует.

**Для s8_b**: использовать `gain_scheduling=True`, `Va_ref=30.0`, ретримироваться
на целевой VA_REF через `compute_trim(aircraft, VA_REF)`.

**Идея на будущее**: добавить speed-dependent feedforward в SpeedController:
  `thr_ff = f(alpha_measured, Va_measured)` — пересчитывать балансировочную тягу
  через модель тяги из `runner.compute_trim` на каждом шаге.

### Формула горизонтальной скорости в земной СК
В `dynamics.py` (x_dot), `state.py` (kinematic_gamma), `sensors.py`
(measure_gps_velocity_earth) используется:
```
Vx_earth = u·cos θ − w·sin θ
```
По B&M должно быть `+w·sin θ`. Следствия:
- **На динамику не влияет**: x не входит в уравнения движения.
- **Траектория в x** немного занижена на больших углах тангажа (ошибка ~5% при θ=15°).
- **gamma_gps завышается** на ~0.5–1° при манёврах с θ > 10°, что вносит
  систематическую ошибку alpha_est даже при нулевом ветре (~сопоставима с шумом зонда).
  Все три места используют одну формулу, поэтому внутренняя согласованность сохранена.
  Не исправлять в одном месте без исправления в остальных двух.
