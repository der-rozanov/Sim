# Симулятор самолётного БПЛА — обзор

Имитационная модель ЛА самолётного типа для главы 8 диссертации  
**«Разработка зонда измерения аэродинамических характеристик БПЛА»**

---

## Назначение

Симулятор решает две задачи:

1. **Доказать тезис**: прямое измерение угла атаки (УА) зондом безопаснее и точнее, чем косвенная оценка `α ≈ θ − γ`. Ключевой эксперимент — парный прогон «с зондом / без зонда» в условиях сдвига ветра по высоте (сценарий С6).

2. **Дать материал для приложения Б**: функция `derivatives()` — это прямая запись системы ОДУ, которую можно перенести в текст диссертации как есть.

---

## Быстрый старт

```bash
# Демонстрационный прогон (тримовый полёт, 4 графика)
python demo.py

# Анимация
python animate.py

# Запустить все тесты
python check.py
```

---

## Структура файлов

```
Симулятор/
│
├── config.py        Параметры ЛА, датчиков, ветра, прогона
├── state.py         Вектор состояния, СК, air_velocity, энергия
├── aero.py          Модель CL / CD / Cm и аэросилы
├── wind.py          Модель ветра: постоянный + сдвиг + порыв
├── dynamics.py      Функция производных (ОДУ системы)
├── integrators.py   RK4 и Эйлер
├── runner.py        Главный цикл, Log, compute_trim, trim_state
│
├── plotting.py      Статические графики (4 функции)
├── animate.py       Анимация по логу (изолирована от симулятора)
├── demo.py          Точка входа: запускает прогон и графики
├── check.py         Тесты здравого смысла по всем модулям
│
└── docs/            ← вы здесь
    ├── README.md           (этот файл)
    ├── physics.md          Физическая модель и уравнения движения
    ├── aerodynamics.md     Аэродинамическая модель CL/CD/Cm
    └── architecture.md     Архитектура кода, зависимости, расширение
```

---

## Параметры по умолчанию

Все числа — **аналог Aerosonde** (Beard & McLain, прил. E).  
Замена на авторские данные: только `config.py`, остальной код не трогать.

Метки `[АВТОР]` в `config.py` показывают, что именно нужно заменить.

---

## Зависимости

```
Python 3.x
numpy
matplotlib
```

Никаких сторонних физических движков.

---

## Статус этапов

| Этап | Статус | Содержание |
|------|--------|------------|
| 0 — Каркас | ✅ Готов | Структура, конфиг, вектор состояния, RK4, пустой прогон |
| 1 — База | 🔄 В работе | Физика, аэро, датчики, ПИД, сценарии С1–С5 |
| 2 — Слой доказательства | ⬜ Ожидает | Ветер, косвенная оценка УА, парный прогон С6 |
| 3 — Фичи | ⬜ Ожидает | Боковой канал, верификация, наблюдатель УА |
